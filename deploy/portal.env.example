# Optional. Copy to /etc/cim-portal/portal.env. Referenced by the systemd unit
# (EnvironmentFile). Use for JVM tuning; DB creds + keys live in application-prod.yml.
JAVA_OPTS=-Xms256m -Xmx512m -XX:+UseG1GC -XX:MaxRAMPercentage=75
