# 图标选择器尺寸修正 + 自定义图标上传 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development(推荐)或 superpowers:executing-plans。步骤用 `- [ ]`。

**Goal:** ① 修正链接表单图标选择器在宽弹窗下被撑大的问题;② 支持上传自定义图标(Oracle BLOB 存储,`link.icon` 用 `upload:<id>` 约定,内置图标不变)。

**Architecture:** 选择器改固定 ~44px 自动填充磁贴。上传图标存 `uploaded_icon` BLOB 表;`POST /api/admin/icons` 存、`GET /api/icons/{id}`(permitAll)取;`AppIcon.vue` 见 `upload:` 前缀渲染 `<img>`。

**Tech Stack:** Vue3/TS/Tailwind v3/vue-i18n/Vitest;Spring Boot 3.3/JDK21/Oracle/Flyway/JPA/Testcontainers。

---

## 契约/现状(已核对)
- 提交:前端→`cim-portal-client@dev`,后端→`cim-portal-server@dev`。trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。前端 `npm run build`(vue-tsc+vite)+ `npm test`(vitest,含 i18n 键对齐 `src/lib/i18n/i18n.spec.ts`);后端 `mvn test`(Testcontainers oracle-free)。
- `AppIcon.vue`:`<component :is="iconFor(name)" :stroke-width="2"/>`(自身无尺寸)。`iconFor(name)=map[name] ?? FALLBACK_ICON`(`src/lib/ui/iconMap.ts`)。
- 选择器 `src/features/admin/links/LinkFormModal.vue` 第 172–176:`<div class="grid grid-cols-6 gap-1.5 sm:grid-cols-12">`,按钮 `class="grid aspect-square w-full place-items-center rounded-lg border transition"` + `:class`(选中/未选)+ 内 `<AppIcon :name="ic" class="size-4"/>`,`@click="form.icon = ic"`。
- 卡片 `src/features/dashboard/SystemCard.vue` 第 30:`<span class="grid size-9 shrink-0 place-items-center rounded-xl bg-brand text-white"><AppIcon :name="link.icon" class="size-[18px]"/></span>`。
- 后端 `Link.icon` `length=64`;`LinkRequest.icon @NotBlank`。`ApiException.badRequest(...)` 为既有错误风格。`SecurityConfig` permitAll 用 `requestMatchers(...)`,`/api/admin/**` → `hasRole(PORTAL_ADMIN)`,末尾 `anyRequest().authenticated()`。`OracleIntegrationTest` 基类 + `TestJwts.bearerFor(empId)`。Flyway oracle 最新 V7;`OracleMigrationTest` 断言 7。
- **硬约束:** 后端 `mvn test` 绿;`OracleMigrationTest`=8;i18n 键对齐;内置图标行为不变;`/api/icons/**` 仅 GET permitAll,上传仍 admin。

---

# 任务 1 — 图标选择器尺寸修正(前端)

**Files:** Modify `cim-portal-client/src/features/admin/links/LinkFormModal.vue`

- [ ] **Step 1:** 改选择器栅格(第 172 行):
  - 旧:`<div class="grid grid-cols-6 gap-1.5 sm:grid-cols-12">`
  - 新:`<div class="grid gap-1.5 [grid-template-columns:repeat(auto-fill,minmax(2.75rem,1fr))]">`
- [ ] **Step 2:** 改按钮(第 174 行)class:去掉 `w-full`,保留 `aspect-square`:
  - 旧:`class="grid aspect-square w-full place-items-center rounded-lg border transition"`
  - 新:`class="grid aspect-square place-items-center rounded-lg border transition"`
- [ ] **Step 3:** 图标放大(第 176 行):`<AppIcon :name="ic" class="size-4" />` → `class="size-5"`。
- [ ] **Step 4:** `cd cim-portal-client && npm run build` 通过;`npm run dev` 打开链接编辑弹窗,确认图标磁贴 ~44px、铺满且换行(截图前后对比交控制器)。
- [ ] **Step 5:提交** `git commit -m "fix(ui): 图标选择器磁贴固定 ~44px 自动填充(修复宽弹窗下被撑大)"`。

---

# 任务 2 — 自定义图标上传

## Task 2.1:后端阅读现状
- [ ] **Step 1:Read** `src/main/java/com/cimportal/link/{Link.java,dto/LinkRequest.java}`、`com/cimportal/auth/SecurityConfig.java`(permitAll 写法 + 链式顺序)、`com/cimportal/common/ApiException.java`、`src/main/resources/application.yml`(是否已有 multipart 配置)、`src/test/java/com/cimportal/support/{OracleIntegrationTest.java,TestJwts.java}`、`src/test/java/com/cimportal/migration/OracleMigrationTest.java`、一个现有 admin 控制器测试(multipart/MockMvc 参考)。

## Task 2.2:Flyway V8
**Files:** Create `src/main/resources/db/migration/oracle/V8__uploaded_icon.sql`
- [ ] **Step 1:**
```sql
CREATE TABLE uploaded_icon (
  id           NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1000),
  content_type VARCHAR2(64)  NOT NULL,
  file_size    NUMBER        NOT NULL,
  bytes        BLOB          NOT NULL,
  created_at   TIMESTAMP     DEFAULT SYSTIMESTAMP NOT NULL,
  CONSTRAINT pk_uploaded_icon PRIMARY KEY (id)
);
```
- [ ] **Step 2:** `OracleMigrationTest` 断言 `7` → `8`。

## Task 2.3:实体 + 仓库
**Files:** Create `src/main/java/com/cimportal/icon/UploadedIcon.java`、`UploadedIconRepository.java`
- [ ] **Step 1:**
```java
package com.cimportal.icon;
import jakarta.persistence.*;
import java.time.Instant;
@Entity @Table(name = "uploaded_icon")
public class UploadedIcon {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY) private Long id;
    @Column(name = "content_type", nullable = false, length = 64) private String contentType;
    @Column(name = "file_size", nullable = false) private long fileSize;
    @Lob @Column(name = "bytes", nullable = false) private byte[] bytes;
    @Column(name = "created_at", nullable = false) private Instant createdAt = Instant.now();
    protected UploadedIcon() {}
    public UploadedIcon(String contentType, long fileSize, byte[] bytes) { this.contentType = contentType; this.fileSize = fileSize; this.bytes = bytes; }
    public Long getId() { return id; }
    public String getContentType() { return contentType; }
    public long getFileSize() { return fileSize; }
    public byte[] getBytes() { return bytes; }
}
```
```java
package com.cimportal.icon;
import org.springframework.data.jpa.repository.JpaRepository;
public interface UploadedIconRepository extends JpaRepository<UploadedIcon, Long> { }
```

## Task 2.4:服务
**Files:** Create `src/main/java/com/cimportal/icon/IconService.java`
- [ ] **Step 1:**
```java
package com.cimportal.icon;
import com.cimportal.common.ApiException;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import org.springframework.web.multipart.MultipartFile;
import java.util.Set;

@Service
public class IconService {
    private static final Set<String> ALLOWED = Set.of("image/png","image/jpeg","image/webp","image/svg+xml");
    private static final long MAX_BYTES = 512L * 1024;
    private final UploadedIconRepository repo;
    public IconService(UploadedIconRepository repo) { this.repo = repo; }

    @Transactional
    public UploadedIcon store(MultipartFile file) {
        if (file == null || file.isEmpty()) throw ApiException.badRequest("文件为空");
        String ct = file.getContentType();
        if (ct == null || !ALLOWED.contains(ct)) throw ApiException.badRequest("不支持的图片类型: " + ct);
        if (file.getSize() > MAX_BYTES) throw ApiException.badRequest("图片超过 512KB");
        try {
            return repo.save(new UploadedIcon(ct, file.getSize(), file.getBytes()));
        } catch (java.io.IOException e) { throw ApiException.badRequest("读取文件失败"); }
    }

    public UploadedIcon get(Long id) {
        return repo.findById(id).orElseThrow(() -> ApiException.notFound("图标不存在"));
    }
}
```
> 若 `ApiException` 无 `notFound`,用现有等价写法(按 Task 2.1 确认;否则返回 `ResponseStatusException(NOT_FOUND)`)。

## Task 2.5:控制器
**Files:** Create `src/main/java/com/cimportal/icon/IconController.java` + `dto/IconUploadResponse.java`
- [ ] **Step 1:**
```java
// dto
package com.cimportal.icon.dto;
public record IconUploadResponse(Long id, String ref) {}
```
```java
package com.cimportal.icon;
import com.cimportal.icon.dto.IconUploadResponse;
import org.springframework.http.*;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.multipart.MultipartFile;

@RestController
public class IconController {
    private final IconService svc;
    public IconController(IconService svc) { this.svc = svc; }

    @PostMapping("/api/admin/icons")
    public ResponseEntity<IconUploadResponse> upload(@RequestParam("file") MultipartFile file) {
        var i = svc.store(file);
        return ResponseEntity.status(HttpStatus.CREATED).body(new IconUploadResponse(i.getId(), "upload:" + i.getId()));
    }

    @GetMapping("/api/icons/{id}")
    public ResponseEntity<byte[]> serve(@PathVariable Long id) {
        var i = svc.get(id);
        return ResponseEntity.ok()
            .contentType(MediaType.parseMediaType(i.getContentType()))
            .cacheControl(org.springframework.http.CacheControl.maxAge(java.time.Duration.ofDays(1)).cachePublic())
            .body(i.getBytes());
    }
}
```

## Task 2.6:安全 + multipart
**Files:** Modify `SecurityConfig.java`、`application.yml`
- [ ] **Step 1:** permitAll 增加 `GET /api/icons/**`(在 `/api/admin/**` 规则与 `anyRequest().authenticated()` 之前):
```java
.requestMatchers(org.springframework.http.HttpMethod.GET, "/api/icons/**").permitAll()
```
（`/api/admin/icons` 不匹配该规则,仍由 `/api/admin/**` 限 admin。）
- [ ] **Step 2:** 基础 `application.yml` 顶部 `spring:` 下加(若无):
```yaml
  servlet:
    multipart:
      max-file-size: 1MB
      max-request-size: 1MB
```

## Task 2.7:后端测试
**Files:** Create `src/test/java/com/cimportal/icon/IconControllerTest.java`
- [ ] **Step 1:** `extends OracleIntegrationTest`,`@Autowired MockMvc`(或 webAppContextSetup,按现有 admin 测试方式);admin token 用 `TestJwts.bearerFor(<PORTAL_ADMIN 工号>)`(参照现有测试如何备 admin)。用例:
  - 合法 PNG(1×1):`multipart("/api/admin/icons").file(new MockMultipartFile("file","i.png","image/png", PNG_1x1)).header("Authorization", admin)` → 201,`$.ref` 形如 `upload:<digits>`;再 `GET /api/icons/{id}` → 200 且 `Content-Type: image/png`、body 非空。
  - 错误类型:`text/plain` → 400。
  - 超限:>512KB 的 `image/png` → 400。
  - 缺失:`GET /api/icons/999999` → 404。
  - 1×1 PNG 字节常量(Base64 解码):
    `iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==`
- [ ] **Step 2:** `cd cim-portal-server && mvn -q test 2>&1 | grep -E 'Tests run:|BUILD' | tail -4` → SUCCESS(含 `OracleMigrationTest` 8 迁移)。
- [ ] **Step 3:提交(后端)** `git commit -m "feat(icon): 自定义图标上传后端(V8 uploaded_icon + 上传/服务端点 + 校验 + 测试)"`。

## Task 2.8:前端 AppIcon + 卡片
**Files:** Modify `cim-portal-client/src/lib/ui/AppIcon.vue`、`src/features/dashboard/SystemCard.vue`;Test `src/lib/ui/AppIcon.spec.ts`
- [ ] **Step 1:** `AppIcon.vue`:
```vue
<script setup lang="ts">
import { computed } from 'vue'
import { iconFor } from './iconMap'
const props = defineProps<{ name: string }>()
const uploadId = computed(() => props.name?.startsWith('upload:') ? props.name.slice('upload:'.length) : null)
const Cmp = computed(() => iconFor(props.name))
</script>
<template>
  <img v-if="uploadId" :src="`/api/icons/${uploadId}`" class="object-contain" alt="" />
  <component v-else :is="Cmp" :stroke-width="2" />
</template>
```
> 父级的尺寸 class(如 `size-5`/`size-[18px]`)经 attribute fallthrough 作用到渲染出的根元素(`<img>` 或 svg),无需额外尺寸。
- [ ] **Step 2:** `SystemCard.vue` 第 30:按是否上传切换底色:
```vue
<span :class="['grid size-9 shrink-0 place-items-center rounded-xl', link.icon?.startsWith('upload:') ? 'border border-border bg-white' : 'bg-brand text-white']">
  <AppIcon :name="link.icon" :class="link.icon?.startsWith('upload:') ? 'size-7' : 'size-[18px]'" />
</span>
```
- [ ] **Step 3:** `AppIcon.spec.ts` 加用例:`mount(AppIcon,{props:{name:'upload:1'}})` → 含 `img` 且 `src` 以 `/api/icons/1` 结尾;`name:'factory'` → 渲染 svg(无 img)。
- [ ] **Step 4:** `npm run build` + `npm test` 通过。

## Task 2.9:前端 选择器上传 + api + i18n
**Files:** Modify `LinkFormModal.vue`、`src/lib/api/*`(图标上传)、`src/lib/i18n/locales/{zh,en}.ts`;
- [ ] **Step 1:Read** 现有 api client 约定(如何发请求/baseUrl),加 `uploadIcon(file: File): Promise<{id:number; ref:string}>`(用 `FormData`,POST `/api/admin/icons`,字段名 `file`;沿用现有鉴权头注入)。
- [ ] **Step 2:** `LinkFormModal.vue` 选择器:
  - 加隐藏 `<input type="file" accept="image/png,image/jpeg,image/webp,image/svg+xml">` + 一个「上传」磁贴按钮(与图标磁贴同尺寸,放网格首位或其上方),点击触发文件选择。
  - `@change`:取文件→`uploadIcon(file)`→`form.icon = res.ref`;失败 toast 错误(沿用现有 toast)。
  - 已选预览:当 `form.icon` 为 `upload:*`,在网格首位渲染一个「已选中」磁贴 `<AppIcon :name="form.icon" class="size-5"/>`(高亮),使编辑态/上传后可见当前自定义图标。
- [ ] **Step 3:** i18n:zh/en 同结构加 `admin.linkForm.uploadIcon`(如「上传图标」/「Upload icon」)+ 上传失败提示键。保持键对齐。
- [ ] **Step 4:** `npm run build` + `npm test`(含 i18n 对齐)通过。
- [ ] **Step 5:提交(前端)** `git commit -m "feat(icon): AppIcon 支持上传图标(upload: 前缀)+ 卡片/选择器渲染 + 上传 UI + i18n"`。

---

## 自检清单(Self-Review)
- **规格覆盖:** 选择器尺寸(任务1);V8(2.2)、实体/仓库(2.3)、服务+校验(2.4)、上传/服务端点(2.5)、安全 permitAll GET + multipart(2.6)、后端测试含 Migration=8(2.7)、AppIcon/卡片渲染(2.8)、选择器上传+api+i18n(2.9)。✔
- **占位符:** 关键文件给完整代码;1×1 PNG 常量已给;「按现状确认」处先 Read(2.1/2.9S1)。无 TBD。
- **一致性:** `upload:<id>` 约定贯穿(后端 ref 2.5 ↔ AppIcon 2.8 ↔ SystemCard 2.8 ↔ 选择器 2.9);`GET /api/icons/{id}` 路径(2.5 ↔ 2.6 permitAll ↔ AppIcon src ↔ api 不涉及);上传字段名 `file`(2.5 ↔ 2.9 api)。
- **硬约束:** `mvn test` 绿(2.7);`OracleMigrationTest`=8(2.2);内置图标行为不变(AppIcon 仅新增 `upload:` 分支,`iconFor` 不变);`/api/icons/**` 仅 GET permitAll、上传仍 admin(2.6);i18n 对齐(2.9)。
- **范围外:** 孤儿图标 GC、图片压缩 —— 不做。
