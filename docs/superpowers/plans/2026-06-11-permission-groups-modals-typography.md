# 权限组 + 响应式弹窗 + 全局字体 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development(推荐)或 superpowers:executing-plans 逐任务实现。步骤用 `- [ ]`。

**Goal:** 在不破坏现有权限管理的前提下新增「权限组」(用户↔组、组↔链接,与现有授权取并集);让所有配置弹窗随窗口自适应尺寸;全局换用自托管 Inter + MiSans 字体并提供预览。

**Architecture:** 权限组复用现有 `link_access_grant`(新增 `GrantType.GROUP`,grant_code=组 code)+ 新 `permission_group`/`permission_group_member` 两表;`PermissionResolver` 增加用户组 code 集合参数并入并集;前端新增组管理页 + 链接表单授权区加 GROUP。弹窗在共享 `Modal.vue` 加 `size` 档位 + `w-[min(92vw,…)]` 钳制。字体自托管 woff2 + `@font-face` + Tailwind `fontFamily.sans`。

**Tech Stack:** Spring Boot 3.3 / JDK 21 / Oracle / Flyway / JPA;Vue 3 / TS / Vite / Tailwind v3 / reka-ui / vue-i18n / Pinia;Testcontainers(oracle-free)、Vitest。

---

## 契约 / 现状(本会话已核对)

**仓库与命令:** 后端 `cim-portal-server`(Oracle,JDK21,`pom.xml`+`src` 在仓库根,分支 `dev`;`mvn test` 用 Testcontainers oracle-free)。前端 `cim-portal-client`(同级仓库;`npm run build`=vue-tsc+vite build;`npm test`=vitest)。提交追加 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。前端改动提交 `cim-portal-client`,后端改动提交 `cim-portal-server`,均在 `dev`。

**现有(不可破坏):**
- `link_access_grant(id, link_id, grant_type ∈ {DEPARTMENT,ROLE}, grant_code)`,FK→`link(id)` ON DELETE CASCADE,唯一 `(link_id,grant_type,grant_code)`。
- `com.cimportal.link.GrantType { DEPARTMENT, ROLE }`。
- `com.cimportal.portal.PermissionResolver.isVisible(String departmentCode, String roleCode, List<LinkAccessGrant> grants)`:`grants` 空 → true;否则逐 grant OR 匹配 dept/role。
- `com.cimportal.portal.HomeService`:批量按 link_id 取 grants,`accessible = isVisible(...)`,不可见时 `HomeLink` 的 `url`/`downloadUrl` 置 null;`accessible` 始终返回。
- `com.cimportal.user.UserInfo(employee_id, display_name_zh, display_name_en, department_code, role_code, email, active, synced_at)`。
- `com.cimportal.link.LinkAdminController`(`/api/admin/links`,含 `GET/POST/PUT/DELETE /{id}/grants`;`GrantRequest{grantType,grantCode}`,`GrantsReplaceRequest{grants}` PUT 整体替换);`LinkService.requireGrantCode(...)` 校验 DEPT/ROLE code 是否在 `enum_value`。
- 管理端点经 Spring Security 限 `hasRole('PORTAL_ADMIN')`(`/api/admin/**`)。
- oracle 迁移 V1..V6(最新 V6)在 `src/main/resources/db/migration/oracle`;`OracleMigrationTest` 断言 `migrationsExecuted == 6`。
- 测试基类 `com.cimportal.support.OracleIntegrationTest`;`com.cimportal.support.TestJwts.bearerFor(employeeId)` 造 `iss=cim-portal` token。
- 前端:`src/features/admin/links/LinkFormModal.vue`(授权行:类型 select `w-28 shrink-0` + code select `flex-1`);admin 视图在 `src/features/admin/{links,enums,users,security}`;用户管理(`UserAdminController` + 前端列表)可作为成员来源;API client 在 `src/lib/api`(沿用现有约定);i18n `src/lib/i18n/locales/{zh,en}.ts` + 键对齐测试 `src/lib/i18n/i18n.spec.ts`。
- 共享弹窗 `src/lib/ui/Modal.vue` 容器:`anim-fade w-full max-w-lg rounded-2xl border border-border bg-white p-5 shadow-2xl max-h-[85vh] overflow-y-auto dark:bg-[#141b2e]`;`EnumFormModal.vue`、`ConfirmDialog.vue` 基于它。
- 字体:无。`tailwind.config.js` 无 `fontFamily`;`index.html`/`src/assets/index.css` 不加载字体。

**硬约束(每步复验):** 后端 `mvn test` 绿;`OracleMigrationTest` 期望数 6→**7**;**无任何 group grant 时现有可见性行为完全不变**;i18n zh/en 键对齐;字体**自托管、零 CDN**;弹窗随窗口缩放自适应且小屏不溢出。

---

# 任务 1 — 权限组(后端 → 前端)

## Task 1.1:阅读现状(不改码)
- [ ] **Step 1:Read** 这些文件全文,确认签名/字段/风格:`src/main/java/com/cimportal/link/{GrantType.java,LinkAccessGrant.java,LinkService.java,LinkAdminController.java}`、`src/main/java/com/cimportal/portal/{PermissionResolver.java,HomeService.java}`、`src/main/java/com/cimportal/user/{UserInfo.java,UserRepository.java 或同名}`、`src/main/resources/db/migration/oracle/V6__security_setting.sql`(IDENTITY/约束写法参照)、`src/test/java/com/cimportal/support/{OracleIntegrationTest.java,TestJwts.java}`、`src/test/java/com/cimportal/link/LinkAdminControllerTest.java`、`src/test/java/com/cimportal/migration/OracleMigrationTest.java`。
- [ ] **Step 2:Read** `src/main/java/com/cimportal/portal/CurrentUser*`/`CurrentUserService*`(获取当前用户 dept/role/employeeId 的方式),记下如何拿到当前 employeeId(供 HomeService 查组)。

## Task 1.2:Flyway V7 迁移
**Files:** Create `src/main/resources/db/migration/oracle/V7__permission_group.sql`

- [ ] **Step 1:** 写迁移:
```sql
CREATE TABLE permission_group (
  id         NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1000),
  code       VARCHAR2(64)  NOT NULL,
  name_zh    VARCHAR2(255) NOT NULL,
  name_en    VARCHAR2(255) NOT NULL,
  active     NUMBER(1)     DEFAULT 1 NOT NULL,
  created_at TIMESTAMP     DEFAULT SYSTIMESTAMP NOT NULL,
  CONSTRAINT pk_perm_group PRIMARY KEY (id),
  CONSTRAINT uk_perm_group_code UNIQUE (code)
);

CREATE TABLE permission_group_member (
  group_id    NUMBER       NOT NULL,
  employee_id VARCHAR2(64) NOT NULL,
  CONSTRAINT pk_pg_member PRIMARY KEY (group_id, employee_id),
  CONSTRAINT fk_pg_member_group FOREIGN KEY (group_id)
      REFERENCES permission_group(id) ON DELETE CASCADE
);
```
- [ ] **Step 2:** `OracleMigrationTest`:把断言 `migrationsExecuted == 6` 改为 `== 7`(改后该测试随其余测试在 Task 1.10 一起跑)。

## Task 1.3:GrantType 增加 GROUP
**Files:** Modify `src/main/java/com/cimportal/link/GrantType.java`
- [ ] **Step 1:** 枚举改为 `public enum GrantType { DEPARTMENT, ROLE, GROUP }`。(列宽 `length=16` 足够。)

## Task 1.4:实体 + 仓库
**Files:** Create `src/main/java/com/cimportal/group/PermissionGroup.java`、`PermissionGroupMember.java`、`PermissionGroupMemberId.java`、`PermissionGroupRepository.java`、`PermissionGroupMemberRepository.java`(新建 `group` 包;包名风格与现有一致)。

- [ ] **Step 1:`PermissionGroup`**
```java
package com.cimportal.group;

import jakarta.persistence.*;
import java.time.Instant;

@Entity @Table(name = "permission_group")
public class PermissionGroup {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY) private Long id;
    @Column(nullable = false, length = 64, unique = true) private String code;
    @Column(name = "name_zh", nullable = false, length = 255) private String nameZh;
    @Column(name = "name_en", nullable = false, length = 255) private String nameEn;
    @Column(nullable = false) private boolean active = true;
    @Column(name = "created_at", nullable = false) private Instant createdAt = Instant.now();

    public Long getId() { return id; }
    public String getCode() { return code; } public void setCode(String c) { this.code = c; }
    public String getNameZh() { return nameZh; } public void setNameZh(String v) { this.nameZh = v; }
    public String getNameEn() { return nameEn; } public void setNameEn(String v) { this.nameEn = v; }
    public boolean isActive() { return active; } public void setActive(boolean v) { this.active = v; }
    public Instant getCreatedAt() { return createdAt; } public void setCreatedAt(Instant v) { this.createdAt = v; }
}
```
- [ ] **Step 2:`PermissionGroupMemberId`**(复合主键)
```java
package com.cimportal.group;
import java.io.Serializable; import java.util.Objects;
public class PermissionGroupMemberId implements Serializable {
    private Long groupId; private String employeeId;
    public PermissionGroupMemberId() {}
    public PermissionGroupMemberId(Long groupId, String employeeId) { this.groupId = groupId; this.employeeId = employeeId; }
    @Override public boolean equals(Object o){ if(this==o)return true; if(!(o instanceof PermissionGroupMemberId t))return false; return Objects.equals(groupId,t.groupId)&&Objects.equals(employeeId,t.employeeId);}
    @Override public int hashCode(){ return Objects.hash(groupId, employeeId);}
}
```
- [ ] **Step 3:`PermissionGroupMember`**
```java
package com.cimportal.group;
import jakarta.persistence.*;
@Entity @Table(name = "permission_group_member")
@IdClass(PermissionGroupMemberId.class)
public class PermissionGroupMember {
    @Id @Column(name = "group_id", nullable = false) private Long groupId;
    @Id @Column(name = "employee_id", nullable = false, length = 64) private String employeeId;
    public PermissionGroupMember() {}
    public PermissionGroupMember(Long groupId, String employeeId) { this.groupId = groupId; this.employeeId = employeeId; }
    public Long getGroupId() { return groupId; } public void setGroupId(Long v) { this.groupId = v; }
    public String getEmployeeId() { return employeeId; } public void setEmployeeId(String v) { this.employeeId = v; }
}
```
- [ ] **Step 4:仓库**
```java
package com.cimportal.group;
import org.springframework.data.jpa.repository.JpaRepository;
import java.util.Optional;
public interface PermissionGroupRepository extends JpaRepository<PermissionGroup, Long> {
    Optional<PermissionGroup> findByCode(String code);
    boolean existsByCode(String code);
    boolean existsByCodeAndActiveTrue(String code);
}
```
```java
package com.cimportal.group;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import java.util.List;
public interface PermissionGroupMemberRepository extends JpaRepository<PermissionGroupMember, PermissionGroupMemberId> {
    List<PermissionGroupMember> findByGroupId(Long groupId);
    void deleteByGroupId(Long groupId);
    // 当前用户所属组的 code 集合(用于解析)
    @Query("select g.code from PermissionGroup g, PermissionGroupMember m " +
           "where m.groupId = g.id and m.employeeId = ?1 and g.active = true")
    List<String> findActiveGroupCodesByEmployeeId(String employeeId);
}
```

## Task 1.5:服务
**Files:** Create `src/main/java/com/cimportal/group/PermissionGroupService.java` + DTO 包 `group/dto/`(`GroupRequest`、`GroupResponse`、`MembersReplaceRequest`)。

- [ ] **Step 1:DTO**
```java
// group/dto/GroupRequest.java
package com.cimportal.group.dto;
import jakarta.validation.constraints.*;
public record GroupRequest(@NotBlank @Size(max=64) String code, @NotBlank String nameZh,
                           @NotBlank String nameEn, Boolean active) {}
```
```java
// group/dto/GroupResponse.java
package com.cimportal.group.dto;
import com.cimportal.group.PermissionGroup;
public record GroupResponse(Long id, String code, String nameZh, String nameEn, boolean active) {
    public static GroupResponse of(PermissionGroup g){ return new GroupResponse(g.getId(),g.getCode(),g.getNameZh(),g.getNameEn(),g.isActive()); }
}
```
```java
// group/dto/MembersReplaceRequest.java
package com.cimportal.group.dto;
import java.util.List;
public record MembersReplaceRequest(List<String> employeeIds) {
    public List<String> safe(){ return employeeIds == null ? List.of() : employeeIds; }
}
```
- [ ] **Step 2:Service**
```java
package com.cimportal.group;
import com.cimportal.group.dto.*;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import org.springframework.web.server.ResponseStatusException;
import org.springframework.http.HttpStatus;
import java.util.*;

@Service
public class PermissionGroupService {
    private final PermissionGroupRepository groups;
    private final PermissionGroupMemberRepository members;
    public PermissionGroupService(PermissionGroupRepository g, PermissionGroupMemberRepository m){ this.groups=g; this.members=m; }

    public List<GroupResponse> list(){ return groups.findAll().stream().map(GroupResponse::of).toList(); }

    public GroupResponse get(Long id){ return GroupResponse.of(require(id)); }

    @Transactional
    public GroupResponse create(GroupRequest r){
        if (groups.existsByCode(r.code())) throw new ResponseStatusException(HttpStatus.CONFLICT, "组 code 已存在");
        PermissionGroup g = new PermissionGroup();
        g.setCode(r.code()); g.setNameZh(r.nameZh()); g.setNameEn(r.nameEn());
        g.setActive(r.active() == null || r.active());
        return GroupResponse.of(groups.save(g));
    }

    @Transactional
    public GroupResponse update(Long id, GroupRequest r){
        PermissionGroup g = require(id);
        if (!g.getCode().equals(r.code()) && groups.existsByCode(r.code()))
            throw new ResponseStatusException(HttpStatus.CONFLICT, "组 code 已存在");
        g.setCode(r.code()); g.setNameZh(r.nameZh()); g.setNameEn(r.nameEn());
        if (r.active() != null) g.setActive(r.active());
        return GroupResponse.of(g);
    }

    @Transactional
    public void delete(Long id){ require(id); groups.deleteById(id); } // 成员随 FK cascade 删;link grant 留待管理员清理

    public List<String> membersOf(Long id){ require(id); return members.findByGroupId(id).stream().map(PermissionGroupMember::getEmployeeId).toList(); }

    @Transactional
    public List<String> replaceMembers(Long id, List<String> employeeIds){
        require(id);
        members.deleteByGroupId(id);
        var distinct = new LinkedHashSet<>(employeeIds);
        for (String emp : distinct) members.save(new PermissionGroupMember(id, emp));
        return new ArrayList<>(distinct);
    }

    private PermissionGroup require(Long id){
        return groups.findById(id).orElseThrow(() -> new ResponseStatusException(HttpStatus.NOT_FOUND, "组不存在"));
    }
}
```

## Task 1.6:管理控制器
**Files:** Create `src/main/java/com/cimportal/group/PermissionGroupAdminController.java`
- [ ] **Step 1:**
```java
package com.cimportal.group;
import com.cimportal.group.dto.*;
import jakarta.validation.Valid;
import org.springframework.http.*;
import org.springframework.web.bind.annotation.*;
import java.util.List;

@RestController
@RequestMapping("/api/admin/permission-groups")
public class PermissionGroupAdminController {
    private final PermissionGroupService svc;
    public PermissionGroupAdminController(PermissionGroupService svc){ this.svc = svc; }

    @GetMapping public List<GroupResponse> list(){ return svc.list(); }
    @GetMapping("/{id}") public GroupResponse get(@PathVariable Long id){ return svc.get(id); }
    @PostMapping public ResponseEntity<GroupResponse> create(@Valid @RequestBody GroupRequest r){
        return ResponseEntity.status(HttpStatus.CREATED).body(svc.create(r));
    }
    @PutMapping("/{id}") public GroupResponse update(@PathVariable Long id, @Valid @RequestBody GroupRequest r){ return svc.update(id, r); }
    @DeleteMapping("/{id}") public ResponseEntity<Void> delete(@PathVariable Long id){ svc.delete(id); return ResponseEntity.noContent().build(); }

    @GetMapping("/{id}/members") public List<String> members(@PathVariable Long id){ return svc.membersOf(id); }
    @PutMapping("/{id}/members") public List<String> replaceMembers(@PathVariable Long id, @RequestBody MembersReplaceRequest req){
        return svc.replaceMembers(id, req.safe());
    }
}
```
> 路径在 `/api/admin/**`,已被 Security 限 `PORTAL_ADMIN`,无需额外注解(与现有 admin 控制器一致——在 Step 中按现状确认)。

## Task 1.7:解析器 + HomeService 并集
**Files:** Modify `PermissionResolver.java`、`HomeService.java`
- [ ] **Step 1:`PermissionResolver`** 加重载(保留旧签名以兼容现有调用/测试):
```java
public static boolean isVisible(String departmentCode, String roleCode,
                                java.util.Set<String> userGroupCodes, java.util.List<LinkAccessGrant> grants) {
    if (grants == null || grants.isEmpty()) return true;
    for (LinkAccessGrant g : grants) {
        switch (g.getGrantType()) {
            case DEPARTMENT -> { if (g.getGrantCode().equals(departmentCode)) return true; }
            case ROLE       -> { if (g.getGrantCode().equals(roleCode)) return true; }
            case GROUP      -> { if (userGroupCodes != null && userGroupCodes.contains(g.getGrantCode())) return true; }
        }
    }
    return false;
}
// 旧两参重载委托:userGroupCodes = 空集
public static boolean isVisible(String departmentCode, String roleCode, java.util.List<LinkAccessGrant> grants) {
    return isVisible(departmentCode, roleCode, java.util.Set.of(), grants);
}
```
- [ ] **Step 2:`HomeService`** 注入 `PermissionGroupMemberRepository`,在算可见性前取当前用户组 code:
```java
java.util.Set<String> groupCodes = new java.util.HashSet<>(
    memberRepo.findActiveGroupCodesByEmployeeId(user.employeeId()));  // employeeId 取法按 Task 1.1 Step2 确认
...
boolean accessible = PermissionResolver.isVisible(user.departmentCode(), user.roleCode(), groupCodes, g);
```
> 若 `user` 对象无 `employeeId()`,用现有获取当前 employeeId 的方式(CurrentUser/Service)。**无 group grant 时行为不变**(GROUP 分支不命中,其余逻辑原样)。

## Task 1.8:GROUP 授权校验
**Files:** Modify `LinkService.java`(`requireGrantCode` 或等价校验处)
- [ ] **Step 1:** 注入 `PermissionGroupRepository`;在校验 grant_code 时按类型分流:
```java
switch (req.grantType()) {
    case DEPARTMENT, ROLE -> requireEnumCode(req.grantType(), req.grantCode()); // 现有逻辑
    case GROUP -> { if (!groupRepo.existsByCodeAndActiveTrue(req.grantCode()))
        throw new ResponseStatusException(HttpStatus.BAD_REQUEST, "权限组不存在或已停用: " + req.grantCode()); }
}
```
> 按现状把现有 enum 校验包进 DEPARTMENT/ROLE 分支;保持现有异常风格。

## Task 1.9:后端测试
**Files:** Create `src/test/java/com/cimportal/group/PermissionGroupAdminControllerTest.java`、`src/test/java/com/cimportal/portal/PermissionResolverTest.java`(若无);扩展 `HomeService` 相关集成测试或新建。

- [ ] **Step 1:解析器单测**(纯单元,不需容器):
```java
package com.cimportal.portal;
import com.cimportal.link.*; import org.junit.jupiter.api.Test;
import java.util.*; import static org.junit.jupiter.api.Assertions.*;
class PermissionResolverTest {
    private LinkAccessGrant grant(GrantType t, String c){ var g=new LinkAccessGrant(); g.setGrantType(t); g.setGrantCode(c); return g; }
    @Test void noGrants_visibleToAll(){ assertTrue(PermissionResolver.isVisible("IT","OPERATOR",Set.of(),List.of())); }
    @Test void groupGrant_memberVisible(){ assertTrue(PermissionResolver.isVisible("X","Y",Set.of("G1"),List.of(grant(GrantType.GROUP,"G1")))); }
    @Test void groupGrant_nonMemberHidden(){ assertFalse(PermissionResolver.isVisible("X","Y",Set.of("G2"),List.of(grant(GrantType.GROUP,"G1")))); }
    @Test void deptStillWorks(){ assertTrue(PermissionResolver.isVisible("IT","Y",Set.of(),List.of(grant(GrantType.DEPARTMENT,"IT")))); }
}
```
> 若 `LinkAccessGrant` 无 setter,用反射或在测试中构造;按 Task1.1 实际 API 调整。
- [ ] **Step 2:控制器集成测试**(`extends OracleIntegrationTest`,仿 `LinkAdminControllerTest`):创建组(201,断言 code)、重复 code(409)、列出、PUT 改名、PUT members 替换(造两个 user_info 或直接传 employeeId 字符串——成员无 FK 到 user_info,可直接传任意 id)、GET members 校验、DELETE(204)。用 `TestJwts.bearerFor(<PORTAL_ADMIN 工号>)`;参照现有测试如何准备管理员身份。
- [ ] **Step 3:Home 集成**:建链接 + `PUT /grants` 加 `{GROUP, "G1"}`;员工 A 在组 G1、员工 B 不在 → A 的 `/api/portal/home` 该链接 `accessible=true & url!=null`,B `accessible=false & url=null`。(参照现有 HomeIntegrationTest 的造数与断言方式。)
- [ ] **Step 4:全量** `cd /home/shane/Code/cim-portal/cim-portal-server && mvn -q test 2>&1 | grep -E 'Tests run:|BUILD' | tail -4` → SUCCESS(含 `OracleMigrationTest` 7 迁移)。
- [ ] **Step 5:提交(后端)** `git add -A && git commit`:`feat(perm): 权限组(V7 + 实体/服务/管理 API + 解析并集 + GROUP 授权校验)`。

## Task 1.10:前端 — API client + i18n
**Files:** Create `cim-portal-client/src/lib/api/permissionGroups.ts`(或按现有 api 组织);Modify `src/lib/i18n/locales/{zh,en}.ts`。
- [ ] **Step 1:Read** 现有某个 admin api 模块(如 links/enums api)与 store 约定,照搬风格。
- [ ] **Step 2:** 新增 API:`listGroups()`、`getGroup(id)`、`createGroup(body)`、`updateGroup(id,body)`、`deleteGroup(id)`、`getMembers(id)`、`replaceMembers(id, employeeIds)`,打到 `/api/admin/permission-groups`。类型 `Group{id,code,nameZh,nameEn,active}`。
- [ ] **Step 3:** i18n:在 zh.ts/en.ts 同结构新增 `admin.permissionGroups.*`(标题、列名、新建/编辑/删除、成员、code/名称/启用、空态、校验提示)与链接表单里 GROUP 选项文案。**两文件键必须一致**。

## Task 1.11:前端 — 权限组管理页
**Files:** Create `src/features/admin/groups/GroupsAdminView.vue` + `GroupFormModal.vue` + `GroupMembersModal.vue`(或合并);Modify admin 路由/导航(按现有 admin 视图注册方式)。
- [ ] **Step 1:Read** `src/features/admin/enums/EnumsAdminView.vue` + `EnumFormModal.vue`(作为列表+弹窗 CRUD 范式)与 admin 导航注册处。
- [ ] **Step 2:** `GroupsAdminView`:表格列(code、中/英文名、启用、成员数/操作);「新建」开 `GroupFormModal`;每行「编辑」「成员」「删除(用 ConfirmDialog)」。
- [ ] **Step 3:** `GroupFormModal`(基于 `Modal`,size 见任务 2,本任务先用默认):字段 code、nameZh、nameEn、active;调 create/update。
- [ ] **Step 4:** 成员管理(`GroupMembersModal`):拉用户列表(复用现有 user 列表 API)+ 多选 employee_id,保存调 `replaceMembers`。
- [ ] **Step 5:** 注册路由 + admin 侧栏入口(i18n 文案)。
- [ ] **Step 6:** `npm run build` 通过(vue-tsc 无类型错)。

## Task 1.12:前端 — 链接表单加 GROUP 授权
**Files:** Modify `src/features/admin/links/LinkFormModal.vue`
- [ ] **Step 1:Read** 现有授权区(类型 select 选项 DEPARTMENT/ROLE 来源、code select 选项来源)。
- [ ] **Step 2:** 类型下拉增加 `GROUP` 选项;当某行 `grantType==='GROUP'` 时,其 code 下拉的选项来自 `listGroups()`(组 code→显示中/英文名)。挂载时拉一次组列表缓存。其余沿用现有 grant 行渲染与 `replaceGrants` 保存。
- [ ] **Step 3:** `npm run build` + `npm test`(i18n 键对齐)通过。
- [ ] **Step 4:提交(前端)** `git add -A && git commit`:`feat(perm): 权限组管理页 + 链接授权支持 GROUP + i18n`。

---

# 任务 2 — 响应式配置弹窗(前端)

**Files:** Modify `cim-portal-client/src/lib/ui/Modal.vue`;Modify `LinkFormModal.vue`、`EnumFormModal.vue`、`GroupFormModal.vue`/`GroupMembersModal.vue`、`ConfirmDialog.vue`(凡用 Modal 处指定 size);Test `src/lib/ui/Modal.spec.ts`。

- [ ] **Step 1:Read** `Modal.vue` 全文,确认 props/defineProps、`DialogContent` class 绑定方式。
- [ ] **Step 2:** 加 `size` prop + 宽度映射:
```ts
const props = withDefaults(defineProps<{ open: boolean; title?: string; size?: 'sm'|'md'|'lg'|'xl' }>(), { size: 'md' })
const maxW = { sm: '28rem', md: '36rem', lg: '48rem', xl: '60rem' } as const
const widthStyle = computed(() => ({ width: `min(92vw, ${maxW[props.size]})` }))
```
- [ ] **Step 3:** `DialogContent` 容器:去掉 `max-w-lg`,改 `:style="widthStyle"`,class 中 `w-full max-w-lg` → 移除(保留 `rounded-2xl border ... p-5 shadow-2xl`),`max-h-[85vh]` → `max-h-[90vh]`,保留 `overflow-y-auto`。即:`anim-fade rounded-2xl border border-border bg-white p-5 shadow-2xl max-h-[90vh] overflow-y-auto dark:bg-[#141b2e]` + `:style="widthStyle"`。
- [ ] **Step 4:** 各调用方传 size:`LinkFormModal` `size="xl"`、`GroupMembersModal` `size="lg"`、`EnumFormModal`/`GroupFormModal` `size="md"`、`ConfirmDialog` `size="sm"`(在 ConfirmDialog 内部把 size 传给 Modal)。
- [ ] **Step 5:组件测试** `Modal.spec.ts`:挂载 `size="xl"`,断言根弹窗 `style` 含 `min(92vw, 60rem)`;默认 `md` 含 `36rem`。(用 `@vue/test-utils` + reka-ui;参照现有组件测试若有。)
- [ ] **Step 6:** `npm run build` + `npm test` 通过;`npm run dev` 截图链接弹窗在宽/窄窗口下的自适应(交给控制器手动验证)。
- [ ] **Step 7:提交** `git commit`:`feat(ui): 弹窗响应式尺寸(size 档位 + min(92vw,…) 钳制 + max-h 90vh)`。

---

# 任务 3 — 全局字体(Inter + MiSans,自托管)(前端)

**Files:** Create `cim-portal-client/src/assets/fonts/`(woff2 文件)+ `src/assets/fonts.css`;Modify `src/assets/index.css`(import + body)、`tailwind.config.js`、`package.json`(若用 @fontsource)。

- [ ] **Step 1:获取字体文件(自托管,零 CDN)。**
  - Inter:`npm i -D @fontsource/inter`,从 `node_modules/@fontsource/inter/files/` 拷贝 `inter-latin-{400,500,600,700}-normal.woff2`(+latin-ext 可选)到 `src/assets/fonts/`;或直接从 rsms/inter 发布版取 woff2。许可 SIL OFL。
  - MiSans:从小米 MiSans 官方免费发布(或 jsDelivr 的 misans woff2)取 `MiSans-Regular/Medium/Semibold` 的 woff2,放 `src/assets/fonts/`。许可:免费商用。
  - 若网络不可达官方源:用 `@fontsource/inter` 解决 Inter;MiSans 从 jsDelivr `misans` 包 woff2 vendoring。**务必本地落盘,不在运行时引用 CDN。**
- [ ] **Step 2:`src/assets/fonts.css`** —— 对每个字重写 `@font-face`(`font-display: swap`),例:
```css
@font-face { font-family:'Inter'; font-weight:400; font-style:normal; font-display:swap;
  src:url('./fonts/inter-latin-400-normal.woff2') format('woff2'); }
/* 500/600/700 同理 */
@font-face { font-family:'MiSans'; font-weight:400; font-style:normal; font-display:swap;
  src:url('./fonts/MiSans-Regular.woff2') format('woff2'); }
/* 500→Medium、600→Semibold 同理 */
```
- [ ] **Step 3:`index.css`** 顶部 `@import './fonts.css';`(在 `@tailwind` 之前或之后按 Vite 处理,确保被打包);`body` 增加 `font-family` 走 Tailwind sans + `-webkit-font-smoothing: antialiased;`(或交给 Tailwind base + `font-sans` 于根节点)。
- [ ] **Step 4:`tailwind.config.js`** `theme.extend.fontFamily`:
```js
fontFamily: { sans: ['Inter','MiSans','system-ui','-apple-system','PingFang SC','Microsoft YaHei','sans-serif'] }
```
并确保根容器/`body` 应用 `font-sans`(如 `App.vue` 根或 `index.css` 的 `body{ font-family: ... }`)。
- [ ] **Step 5:** `npm run build` 通过;`npm run dev` 截图 **登录页 / 首页 / 一个管理弹窗**(新字体),交控制器/用户确认。
- [ ] **Step 6:提交** `git commit`:`feat(ui): 全局字体 Inter + MiSans(自托管 woff2 + @font-face + Tailwind sans)`。

---

## 自检清单(Self-Review)

**规格覆盖(spec ①②③):**
- ① 权限组:V7 双表(1.2)、GrantType.GROUP(1.3)、实体/仓库(1.4)、服务(1.5)、管理 API CRUD+成员替换(1.6)、解析并集 + Home 取组(1.7)、GROUP 校验(1.8)、后端测试含 Migration=7 与 Home 集成(1.9)、前端 api+i18n(1.10)、组管理页(1.11)、链接表单 GROUP(1.12)。✔
- ② 弹窗:Modal size + `min(92vw,…)` + max-h-90vh,各调用方指定 size,组件测试(任务 2)。✔
- ③ 字体:自托管 woff2 + @font-face + Tailwind sans + 全局应用 + 预览截图(任务 3)。✔

**占位符扫描:** 关键文件均给完整代码;前端页/字体步骤含具体类名、配置、@font-face 模板与文件名;凡“按现状确认”处均先 Read(1.1/1.10S1/1.11S1/1.12S1/2S1)。无 TBD。

**类型/签名一致:** `GrantType{DEPARTMENT,ROLE,GROUP}`(1.3 ↔ 1.7/1.8 switch);`PermissionResolver.isVisible(dept,role,Set<String>,List)` 新签名 + 旧两参委托(1.7,旧测试/调用不破);`findActiveGroupCodesByEmployeeId`(1.4 ↔ 1.7);`GroupResponse{id,code,nameZh,nameEn,active}`(1.5 ↔ 前端类型 1.10);`replaceMembers/PUT /{id}/members`(1.6 ↔ 1.5 ↔ 前端 1.11);Modal `size:'sm'|'md'|'lg'|'xl'`→`28/36/48/60rem`(任务2 一致)。

**硬约束复验:** `mvn test` 绿(1.9S4);`OracleMigrationTest==7`(1.2S2);**无 group grant 行为不变**——GROUP 分支不命中且旧两参重载保持原逻辑(1.7);i18n 键对齐 `i18n.spec.ts`(1.10S3/1.12S3);字体**自托管零 CDN**(3S1 明确落盘);弹窗 `min(92vw,…)` 随窗口缩放且小屏不溢出(2S3)。

**提交边界:** 后端(1.2–1.9)提交 `cim-portal-server`;前端(1.10–1.12、任务2、任务3)提交 `cim-portal-client`;均 `dev`。
